// Type stubs for internal MediaPipe WASM loader modules.
// These files have no TypeScript declarations — we only import them
// for their resolved URL to derive the WASM base path.
declare module '@mediapipe/tasks-vision/vision_wasm_internal.js' {
  const content: any;
  export default content;
}